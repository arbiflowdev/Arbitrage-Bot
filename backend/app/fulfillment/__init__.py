"""Milestone 4 — hybrid inventory & JIT fulfillment.

Pure, I/O-free building blocks for the fulfillment pipeline (the upload parser
and sourcing decision live here). Orchestration that touches the database,
marketplaces, wallet, and Redis lives in ``app/services`` and ``app/workers``.
"""
